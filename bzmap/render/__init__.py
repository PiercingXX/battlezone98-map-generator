"""Top-down renders of a generated map (docs/05 ``render/``).

- :mod:`bzmap.render.preview` — shaded heightmap render with object,
  connectivity, and economy overlays (the ``preview.png`` in docs/06
  Reporting and the per-map ``.png`` in docs/07).
- :mod:`bzmap.render.thumbnail` — workshop item ``.png`` / ``.BMP``
  thumbnail generation (docs/07 pack layout).
"""